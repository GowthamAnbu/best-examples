import { NotEqualValidatorDirective } from './equal-validator.directive';

describe('EqualValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NotEqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
